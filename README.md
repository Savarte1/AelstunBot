# AelstunBot


## Configuration

Create a `config.toml` file with the following
```toml
[bot]
token = "" # bot token
prefix = "" # bot command prefix

[nationstates]
agent = "" # user agent for NS
key = ""
```